# math

# import match - toda funcionalidade

# form match import sqrt - importa só a funcionalidade da raiz quadrada
import math
num = int(input('Digite um numero: '))
raiz = math.sqrt(num)
print(f'A raiz de {num} é igual a {math.ceil(raiz)}') # arredondou pra cima
print(f'A raiz de {num} é igual a {raiz:.2f}')

# from match import sqrt - - se vc digitar from match + CRTL + Espaco vai dar as opcoes

